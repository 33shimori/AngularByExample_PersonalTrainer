div(id="left-nav-exercises", ng-controller="exercisesNavCtrl")
	h4 Exercises
	div(ng-repeat="exercise in exercises|orderBy: 'title'", class="row)
		button(class="btn btn-info col-sm-12", ng-click="addExercise(exercise)"){{exercise.title}}
			span(class="glyphicon glyphicon-chevron-right")

